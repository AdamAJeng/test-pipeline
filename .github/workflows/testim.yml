name: Testim E2E
on: [push]

jobs:
  run-testimio-cli:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v4.2.2

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4.4.0
      with:
        node-version: 23

    - name: 📦 Install dependencies
      run: |
        npm install -g testim
        npm install -g commander # Add commander to fix the error
        sudo apt-get install jq # Ensure jq is available as a CLI tool

    - name: 🧪 Run Testim Tests
      run: |
        chmod +x run-tests-on-grid.sh
        ./run-tests-on-grid.sh